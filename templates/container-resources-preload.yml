{% for testenv in testenvs %}
- name: container-{{ testenv.name }}-base
  type: registry-image
  source:
    repository: quay.io/spearow/machine-learning-container-{{ testenv.name }}-base
    username: ((quay-spearow-username))
    password: ((quay-spearow-password))
    # max_concurrent_downloads: 4
    # max_concurrent_uploads: 2
{% endfor %}