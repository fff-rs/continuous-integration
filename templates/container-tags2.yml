{% let name = backend.name.as_str() %}
{%- match name %}
{%- when "cuda" %}
      tags: [framework:cuda]
      # privileged: true # FIXME this must be replaced with some proper selinux rules
{%- when "opencl" %}
      #tags: [framework:opencl]
{%- when "default" %}
      tags: [framework:opencl,framework:cuda]
      # privileged: true # FIXME this must be replaced with some proper selinux rules
{%- else %}
{%- endmatch %}
