{% let name = backend.name.as_str() %}
{%- match name %}
{%- when "cuda" %}
          tags: [framework:cuda]
{%- when "opencl" %}
          # tags: [framework:opencl]
          tags: []
{%- when "default" %}
          tags: [framework:opencl,framework:cuda]
{%- else -%}
{%- endmatch %}
{# ALSO MODIFY CONTAINER TAGS ACCORDINGLY! #}
