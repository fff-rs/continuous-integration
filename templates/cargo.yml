                prepare
                cargo-override-injection
                export RUST_LOG=juice=debug
                export RUST_BACKTRACE=1
{%- if backend.execute == BackendExecute::Build -%}
{%- if backend.name == "default" %}
                cargo build
{%- else if backend.name == "native" %}
                cargo build --no-default-features --features=native
{%- else %}
                cargo build --no-default-features --features=native,{{ backend.name }}
{%- endif %}
{%- else -%}
{%- if backend.name == "default" %}
                cargo test -- --nocapture
{%- else if backend.name == "native" %}
                cargo test --no-default-features --features=native -- --nocapture
{%- else %}
                cargo test --no-default-features --features=native,{{ backend.name }} -- --nocapture
{%- endif %}
{%- endif -%}
