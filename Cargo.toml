[package]
name = "concourse-matrix"
version = "0.1.0"
authors = ["Bernhard Schuster <bernhard@ahoi.io>"]

[dependencies]
askama = "0.8"
regex = "0.2"
lazy_static = "0.2"
error-chain = "0.11"

[build-dependencies]
askama = "0.8"
