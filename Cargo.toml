[package]
name = "concourse-matrix"
version = "0.1.0"
authors = ["Bernhard Schuster <bernhard@ahoi.io>"]
edition = "2018"

[dependencies]
askama = "0.8"
regex = "1"
lazy_static = "1"
error-chain = "^0.11"

[build-dependencies]
askama = "0.8"
